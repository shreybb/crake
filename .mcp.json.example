{
  "mcpServers": {
    "playwright": {
      "command": "playwright-mcp",
      "args": [
        "--user-data-dir=~/.playwright-crake-profile"
      ],
      "description": "Optional: headed browser automation for Benchling. Copy to .mcp.json and adjust paths. Not required to run Crake."
    }
  }
}
