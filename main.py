"""
Crake — deterministic plasmid design workbench.

Run the UI:
    uv run streamlit run app.py

Run the CLI:
    uv run crake --help
    uv run crake cmd "/help"

See docs/cli.md and docs/scope.md.
"""
